name: app_features_about
description: "This package provides about-related features for the application."
publish_to: "none"
version: 0.0.1
homepage: "https://github.com/FlutterKaigi/2024/tree/main/packages/app/features/about"

environment:
  sdk: ^3.6.0-216.1.beta
  flutter: ^3.26.0-0.1.pre

dependencies:
  app_cores_core:
    path: ../../cores/core
  app_cores_designsystem:
    path: ../../cores/designsystem
  app_cores_settings:
    path: ../../cores/settings
  common_data:
    path: ../../../common/data
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  gap: ^3.0.1
  hooks_riverpod: ^2.5.2
  intl: ^0.19.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
